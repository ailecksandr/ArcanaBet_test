.card.bg-light
  %article.card-body.mx-auto{:style => "max-width: 400px;"}
    %h4.card-title.mt-3.text-center Create Account
    %p.text-center Get started with your free account
    = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = f.error_notification
      .form-group.input-group
        .input-group-prepend
          %span.input-group-text
            %i.fa.fa-envelope
        = f.input :email, required: true, autofocus: true, class: 'form-control', placeholder: 'Email'
      .form-group.input-group
        .input-group-prepend
          %span.input-group-text
            %i.fa.fa-lock
        = f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length), class: 'form-control', placeholder: 'Password'
      .form-group.input-group
        .input-group-prepend
          %span.input-group-text
            %i.fa.fa-lock
        = f.input :password_confirmation, required: true, class: 'form-control', placeholder: 'Repeat password'
      .form-group
        = f.button 'Create Account', class: 'btn btn-primary btn-block'
